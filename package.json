{
  "name": "modforge-market",
  "version": "1.0.0",
  "description": "Minecraft mods and plugins storefront with Stripe Checkout.",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check success.js",
    "test": "node --test --test-isolation=none tests/server.test.js"
  }
}
